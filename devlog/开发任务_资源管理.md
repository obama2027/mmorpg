# 资源管理开发任务

## 当前已实现功能

### 1. AssetBundle 运行时管理

- 已实现 `AssetBundleManager` 单例。
- 正式环境统一走 `AssetBundle` 异步加载。
- 支持 `AssetBundleManifest` 依赖解析与依赖自动加载。
- 支持 Windows、Android、iOS 平台路径处理。
- Android 运行时使用 `UnityWebRequestAssetBundle`。
- PC/iOS 运行时使用 `AssetBundle.LoadFromFileAsync`。
- 支持已加载 bundle 复用与引用计数管理。
- 支持 bundle 卸载与 `UnloadUnusedAssets` 内存回收。

### 2. 开发环境与正式环境切换

- 支持 `developmentMode` 开关。
- Unity Editor 开发模式下可直接从源资源路径加载。
- 正式环境 `AssetBundle` 模式下限制同步加载接口，避免同步/异步混用导致状态错乱。

### 3. 资源加载接口

- 支持 `AssetAddress` 资源地址定义。
- 支持 `LoadAssetAsync<T>()` 异步资源加载。
- 支持 `AssetHandle<T>` 句柄持有与释放。
- 同步加载接口仅保留给 Editor 开发模式调试使用。

### 4. 场景加载接口

- 支持 `SceneAddress` 场景地址定义。
- 支持 `BundleSceneLoader.LoadSceneAsync()` 场景异步加载。
- 支持场景卸载后自动释放对应 scene bundle。
- 支持 Editor 开发模式下直接加载源场景。

### 5. 预热系统

- 支持 bundle 级预热。
- 支持 asset 级预热。
- 支持分组预热 `PreloadGroupConfig`。
- 支持分组释放、bundle 释放、asset 释放。
- 支持预热进度回调。
- 支持预热资源缓存查询。

### 6. 分包下载扩展点

- 已抽象 `IBundlePackageService` 接口。
- 已提供 `LocalBundlePackageService` 本地直通实现。
- 后续可无侵入接入分包下载、校验和缓存管理。

### 7. 配置系统

- 已实现 `AssetAddressConfig`，统一维护资源 key 到地址映射。
- 已实现 `PreloadGroupConfig`，统一维护分组、分包、预热资源配置。
- 已提供 `AssetAddressConfigValidator` 校验工具。
- 已提供 `PreloadGroupConfigValidator` 校验工具。

### 8. 编辑器构建工具

- 已实现 `AssetBundleBuilder`。
- 支持按当前平台构建 AssetBundle。
- 支持 Android、iOS、Windows64 单独构建。
- 输出目录统一为 `Assets/StreamingAssets/AssetBundles/<Platform>/`。

### 9. 调试与稳定性

- 已实现统一错误码 `BundleErrorCode`。
- 已实现统一异常类型 `BundleException`。
- 已实现统一日志入口 `BundleLogger`。
- 已实现加载、预热、释放全链路日志。
- 已实现重复释放和非法释放保护。
- 已提供当前已加载 bundle、预热 bundle、预热 asset、预热 group 的调试查询接口。

### 10. 对外门面

- 已实现 `GameAssetService` 统一资源服务入口。
- 外部模块可通过该门面进行资源加载、场景加载、预热、释放和调试查询。

## 当前目录实现文件

- `client/Assets/Scripts/Core/Bundle/AssetAddress.cs`
- `client/Assets/Scripts/Core/Bundle/SceneAddress.cs`
- `client/Assets/Scripts/Core/Bundle/AssetHandle.cs`
- `client/Assets/Scripts/Core/Bundle/BundleErrorCode.cs`
- `client/Assets/Scripts/Core/Bundle/BundleException.cs`
- `client/Assets/Scripts/Core/Bundle/BundleLogger.cs`
- `client/Assets/Scripts/Core/Bundle/BundleDebugModels.cs`
- `client/Assets/Scripts/Core/Bundle/BundlePathUtility.cs`
- `client/Assets/Scripts/Core/Bundle/IBundlePackageService.cs`
- `client/Assets/Scripts/Core/Bundle/LocalBundlePackageService.cs`
- `client/Assets/Scripts/Core/Bundle/AssetAddressConfig.cs`
- `client/Assets/Scripts/Core/Bundle/PreloadGroupConfig.cs`
- `client/Assets/Scripts/Core/Bundle/AssetBundleManager.cs`
- `client/Assets/Scripts/Core/Bundle/BundleSceneLoader.cs`
- `client/Assets/Scripts/Core/Bundle/PreloadService.cs`
- `client/Assets/Scripts/Core/Bundle/GameAssetService.cs`
- `client/Assets/Scripts/Core/Bundle/AssetAddressConfigValidator.cs`
- `client/Assets/Scripts/Core/Bundle/PreloadGroupConfigValidator.cs`
- `client/Assets/Scripts/Core/Bundle/AssetBundleBuilder.cs`

## 后续建议优先开发功能

### P1

- `AssetAddressConfig` 自动生成工具，减少手工录入 key 和路径。
- `PreloadGroupConfig` 自动生成或辅助编辑工具。
- 资源类型字段补充，支持按配置自动决定加载类型，减少 `UnityEngine.Object` 泛型兜底。
- 登录、主城、战斗模块的首批分组配置资产落地。
- 资源加载超时、场景切换失败兜底和提示流程。

### P2

- 分包下载正式实现，包括远端版本、下载、校验、落盘缓存。
- Bundle 本地缓存目录管理与版本清理策略。
- 运行时调试面板，展示当前已加载 bundle 与引用计数。
- 预热与加载性能统计，记录耗时、命中率和峰值数量。
- AB 命名规范校验与资源目录规范工具。

### P3

- 资源下载断点续传。
- CDN 热更新与资源版本回滚策略。
- 更细粒度的内存分析与分组回收策略。
- 场景分块加载与区域流式资源管理。
- 结合 HybridCLR 的热更资源初始化流程整合。

## 当前建议

- 现在这套实现已经可以进入正式开发接入阶段。
- 建议先完成 `AssetAddressConfig`、`PreloadGroupConfig` 以及登录模块分组配置。
- 建议优先接入登录模块、公共 UI 图集和主城入口流程。
- 正式环境中继续坚持 bundle 只走异步加载。
